'use client';

/** Placeholder seam; replaced in a later phase. */
export function EntityPanel() {
  return (
    <div className="p-4 text-sm text-muted" data-placeholder="entity">Entity profile loads in Phase 4</div>
  );
}

export default EntityPanel;
