'use client';

/** Placeholder seam; replaced in a later phase. */
export function MapView() {
  return (
    <div className="p-4 text-sm text-muted" data-placeholder="map">Map loads in Phase 4</div>
  );
}

export default MapView;
