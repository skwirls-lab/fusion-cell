'use client';

/** Placeholder seam; replaced in a later phase. */
export function GraphView() {
  return (
    <div className="p-4 text-sm text-muted" data-placeholder="graph">Link chart loads in Phase 4</div>
  );
}

export default GraphView;
