'use client';

/** Placeholder seam; replaced in a later phase. */
export function FeedView() {
  return (
    <div className="p-4 text-sm text-muted" data-placeholder="feed">Live feed loads in Phase 4</div>
  );
}

export default FeedView;
