'use client';

/** Placeholder seam; replaced in a later phase. */
export function AnalystPanel() {
  return (
    <div className="p-4 text-sm text-muted" data-placeholder="analyst">AI Analyst loads in Phase 5</div>
  );
}

export default AnalystPanel;
